import { MainLayout } from '@/components/layout'

function CertificacionesLayout({ children }: { children: React.ReactNode }) {
  return <MainLayout>{children}</MainLayout>
}

export default CertificacionesLayout
